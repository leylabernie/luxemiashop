interface CeremonyVerseLinkBlockProps {
  variant?: 'section' | 'compact';
}

/**
 * Retained for backward-compatible imports. LuxeMia custom-order enquiries
 * remain within the LuxeMia storefront, so no external promotion is rendered.
 */
const CeremonyVerseLinkBlock = (_props: CeremonyVerseLinkBlockProps) => null;

export default CeremonyVerseLinkBlock;
